<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= check_box_tag 'completed', true %> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to('Add a new Task', new_task_path) %>
